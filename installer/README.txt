This folder will contain the MSI when built.
Run from repo root:
  powershell -NoProfile -ExecutionPolicy Bypass -File .\build.ps1 -SelfContained -SingleFile
  (Optional) run WiX build script to package MSI (to be added).


